{
  "name": "@xelah/core",
  "description": "A React Library for simple text editing.",
  "version": "1.0.0",
  "homepage": "https://xelah-core.netlify.app/",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/xelahjs/xelah.git"
  },
  "dependencies": {
    "contenteditable-arrow-navigation": "^0.3.0",
    "use-deep-compare": "^1.1.0"
  },
  "scripts": {
    "start": "yarn && styleguidist server",
    "build-dev": "styleguidist build",
    "build": "babel src --out-dir build",
    "test": "react-scripts test",
    "prepublishOnly": "rm -fr ./build && nx build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "peerDependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "main": "build/index.js",
  "files": [
    "build"
  ]
}
